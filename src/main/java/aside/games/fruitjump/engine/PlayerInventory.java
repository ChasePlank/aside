package aside.games.fruitjump.engine;

/**
 * Player inventory: keys, etc.
 * 
 * Simple integer counters. Could be extended with items,
 * equipment, quest flags, etc.
 */
public class PlayerInventory {
    public int keys = 0;
    public int coins = 0;  // currency (top-down prototype; shop later)
}
